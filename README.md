# 🎲 Dice Rolling Simulator
A simple Python program that simulates rolling pairs of dice.

## 📌 About the Project

This project was created as a beginner Python project to practice basic programming concepts such as loops, conditional statements, user input, and the `random` module.

## ✨ Features

* Roll a pair of dice multiple times
* Choose how many pairs to roll
* Generates random values from 1 to 6
* Keeps track of the total number of pairs thrown
* Handles invalid user choices
* Allows the user to continue rolling until they choose `N`

## 🛠️ Technologies Used

* Python
* `random` module

## ▶️ How to Run

1. Make sure Python is installed on your computer.
2. Download or clone this repository.
3. Run the following command:

```bash
python dice_rolling.py
```

4. Follow the instructions displayed in the terminal.

## 🎯 Example

```text
Do you want to roll a pair of dice? (Y/N): y
How many pairs?: 2

( 4 , 6 )
( 2 , 3 )

Do you want to roll a pair of dice? (Y/N): n
Thanks for playing!!
Total pairs thrown: 2 times
```

## 📚 What I Learned

Through this project, I practiced:

* `while` loops
* `for` loops
* `if-elif-else` statements
* Taking input from users
* Using the `random` module
* Using `random.randint()`
* Keeping track of values using variables

## 🚀 Future Improvements

Some features I may add in the future:

* Calculate the total of each pair
* Detect when both dice show the same number
* Add a scoring system
* Display statistics of the rolls

---

Made with Python 🎲

